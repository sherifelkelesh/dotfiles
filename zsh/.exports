#
# Config
# ---------------------------------------------------------------------

# -- APT-GET ----------------------
APTGET="sudo apt-get"

# -- Directories ------------------
BACKUP="$HOME/.backup"
DOTFILES="$HOME/.dotfiles"

DEV="$HOME/dev"
PROJECTS="$DEV/projects"
REPOS="$DEV/repos"

DESKTOP="$HOME/Desktop"
DOCUMENTS="$HOME/Documents"
DOWNLOADS="$HOME/Downloads"
DROPBOX="$HOME/Dropbox"
SOFTWARES="$HOME/.softwares"

# -- Services ---------------------
SERVICE="sudo service"

# Nginx / PHP / MySQL
SERVICE_NGINX="$SERVICE nginx"
SERVICE_PHP="$SERVICE php5-fpm"
SERVICE_MYSQL="$SERVICE mysql-server"
