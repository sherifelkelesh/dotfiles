#
# Aliases
# ---------------------------------------------------------------------

# -- APT-GET ----------------------
alias autoclean="$APTGET autoclean"
alias autoremove="$APTGET autoremove -y"
alias clean="$APTGET clean"
alias dist-upgrade="$APTGET dist-upgrade -y"
alias install="$APTGET install -y"
alias remove="$APTGET remove -y"
alias update="$APTGET update"
alias upgrade="$APTGET upgrade -y"
alias superup="update && upgrade && dist-upgrade"
alias superclean="clean && autoclean && autoremove"

# -- Directories ------------------
alias dev="cd $DEV"
alias docs="cd $DOCUMENTS"
alias downs="cd $DOWNLOADS"
alias dropbox="cd $DROPBOX"
alias projs="cd $PROJECTS"
alias repos="cd $REPOS"
alias softs="cd $SOFTWARES"

# -- Dotfiles ---------------------
alias dots="cd $DOTFILES"
alias dotsconfig="cd $DOTFILES && $EDITOR ."

# -- General Shortcuts ------------
alias c="cat"
alias i="install"
alias s="subl"
alias t="top"
alias v="vim"

# -- NPM --------------------------
alias npm-clean="rm -rf node_modules && npm cache clear"
alias npm-clean-i="npm-clean && npm i"

# -- Server PHP -------------------
# Nginx / PHP / MySQL
alias server-start="$SERVICE_NGINX start && $SERVICE_MYSQL start && $SERVICE_PHP start"
alias server-restart="$SERVICE_NGINX restart && $SERVICE_MYSQL restart && $SERVICE_PHP restart"
alias server-stop="$SERVICE_NGINX stop && $SERVICE_MYSQL stop && $SERVICE_PHP stop"

# -- Stopwatch --------------------
alias timer="echo 'Timer started. Stop with Ctrl-D.' && date && time cat && date"

# -- Top --------------------------
alias top="htop"

# -- Trash ------------------------
alias t="trash"
alias e="empty-trash"

# -- Workflow ---------------------
alias work=""

# -- ZSH & Oh-My-Zsh --------------
alias reload=". ~/.zshrc"
alias zshconfig="$EDITOR $HOME/.zshrc"
alias ohmyzsh="$EDITOR $HOME/.oh-my-zsh"
