#
# Aliases
# ---------------------------------------------------------------------

# -- APT-GET ----------------------
alias autoclean="sudo apt-get autoclean"
alias autoremove="sudo apt-get autoremove -y"
alias check="sudo apt-get check"
alias clean="sudo apt-get clean"
alias dist-upgrade="sudo apt-get dist-upgrade -y"
alias install="sudo apt-get install -y"
alias purge="sudo apt-get purge -y"
alias remove="sudo apt-get remove -y"
alias update="sudo apt-get update"
alias upgrade="sudo apt-get upgrade -y"

alias up="update && upgrade && dist-upgrade"
alias cleanup="clean && autoclean && autoremove"

# -- Directories ------------------
alias dev="cd $DEV"
alias desktop="cd $DESKTOP"
alias documents="cd $DOCUMENTS"
alias downloads="cd $DOWNLOADS"
alias dropbox="cd $DROPBOX"
alias projects="cd $PROJECTS"
alias repos="cd $REPOS"
alias softwares="cd $SOFTWARES"

alias dlist="dirs -v | head -10"

# -- Directories Shortcuts --------
alias d="dev"
alias dt="desktop"
alias dc="documents"
alias dl="downloads"
alias db="dropbox"
alias pr="projects"
alias rp="repos"
alias sw="softwares"

# -- Dotfiles ---------------------
alias dots="cd $DOTFILES"
alias dotsconf="cd $DOTFILES && $EDITOR ."
alias dotsup="source $DOTFILES_BIN/upgrade.sh"
alias dotsbackup="source $DOTFILES_BIN/backup.sh"
alias dotscolors="source $DOTFILES_BIN/colors.sh"

# -- General commands -------------
alias service="sudo service"
alias top="htop"

# -- General Shortcuts ------------
alias c="pygmentize -O style=monokai -f console256 -g"
alias h="history"
alias i="install"
alias r="reload"
alias t="top"

# -- Hosts ------------------------
alias hosts='sudo $EDITOR /etc/hosts'

# -- NPM --------------------------
alias npm-clean="rm -rf node_modules"
alias npm-clean-install="npm-clean && npm i"
alias npmc="npm-clean"
alias npmci="npm-clean-install"

# -- Stopwatch --------------------
alias timer="echo 'Timer started. Stop with Ctrl-D.' && date && time cat && date"

# -- Workflow ---------------------
alias work=""

# -- ZSH & Oh-My-Zsh --------------
alias reload=". ~/.zshrc"
alias zsh="$EDITOR $HOME/.zshrc"
alias ohmyzsh="$EDITOR $HOME/.oh-my-zsh"
